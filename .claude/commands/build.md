Build and verify the project compiles cleanly.

Run `go build ./...` from the `core/` directory. If it fails, analyze the errors and fix them. Do not just report the errors — resolve them, then re-run the build to confirm it passes.
